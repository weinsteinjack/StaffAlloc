# StaffAlloc Frontend - Production Environment Configuration
# Copy this file to .env.production and configure for your AWS deployment

# API Base URL - Update this with your EC2 instance public IP or domain
# Format: https://YOUR-EC2-PUBLIC-IP or https://api.yourdomain.com
VITE_API_BASE_URL=https://YOUR-EC2-PUBLIC-IP

# Alternative: If using a domain name
# VITE_API_BASE_URL=https://api.yourdomain.com

# CloudFront Distribution URL (informational, not used by app directly)
# VITE_CLOUDFRONT_URL=https://YOUR-CLOUDFRONT-DISTRIBUTION.cloudfront.net

# Environment
VITE_ENVIRONMENT=production

# Feature flags (if needed)
# VITE_ENABLE_AI_FEATURES=true

# Production Deployment Notes:
# 1. Replace YOUR-EC2-PUBLIC-IP with actual EC2 instance IP or domain
# 2. Ensure HTTPS is used for API calls
# 3. This file should be renamed to .env.production before running 'npm run build'
# 4. The frontend will be served from CloudFront (S3)
# 5. Ensure CORS is configured in backend to allow CloudFront URL

